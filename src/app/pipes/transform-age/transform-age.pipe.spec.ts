import { TransformAgePipe } from './transform-age.pipe';

describe('TransformAgePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformAgePipe();
    expect(pipe).toBeTruthy();
  });
});
