import { ControlTemplateDataModel } from './control-template-data-model';

describe('ControlTemplateDataModel', () => {
  it('should create an instance', () => {
    expect(new ControlTemplateDataModel()).toBeTruthy();
  });
});
